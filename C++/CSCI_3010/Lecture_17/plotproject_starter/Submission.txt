2.1: double width = view->frameSize().width();double height = view->frameSize().height();scene->addLine(QLineF(0,height/2,width,height/2),QPen()); // X-axisscene->addLine(QLineF(width/2,0,width/2,height),QPen()); // Y-axis3.1:It inherits QMainWindow3.2:QApplication and PlotWindow are  the objects created  and show() and exec() are the methods run. The show() method pulls up the window of our project. The exec() function executes the main.cpp function and runs all the functions.